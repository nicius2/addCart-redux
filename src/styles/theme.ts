export type ThemeType = {
     colors: {
          primary: string
          secundary: string
     },

     font: "'Inter', sans-serif"
}

export const theme: ThemeType = {
     colors: {
          primary: '#fffff',
          secundary: '#D83F22'
     },
     font: "'Inter', sans-serif",
} 